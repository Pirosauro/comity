{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "/docs/00-constitution/comity-module.schema.json",
  "title": "Comity Module Configuration Schema",
  "description": "Schema for validating Comity module declarations per Quality Domains Standard §1.2.1",
  "type": "object",
  "required": ["name"],
  "properties": {
    "name": {
      "type": "string",
      "pattern": "^(@comity/|[a-z0-9\\-@/]+)$",
      "description": "Module name, preferably scoped with @comity/"
    },
    "comity": {
      "type": "object",
      "required": ["qualityDomain"],
      "properties": {
        "qualityDomain": {
          "type": "string",
          "enum": ["core", "strategic", "experimental"],
          "description": "Quality Domain declaration per Quality Domains Standard §1.3.1"
        },
        "declared": {
          "type": "string",
          "format": "date",
          "description": "Date when Quality Domain was declared (YYYY-MM-DD)"
        },
        "owner": {
          "type": "string",
          "pattern": "^@[a-z0-9\\-_/]+$",
          "description": "Module owner team or individual"
        },
        "notes": {
          "type": "string",
          "description": "Optional notes about Quality Domain choice"
        }
      },
      "additionalProperties": false,
      "errorMessage": {
        "required": {
          "qualityDomain": "Missing required property: comity.qualityDomain (per Quality Domains Standard §1.1.1)"
        },
        "properties": {
          "qualityDomain": "qualityDomain must be one of: core, strategic, experimental (per Quality Domains Standard §1.3.1)"
        }
      }
    }
  },
  "additionalProperties": true,
  "if": {
    "properties": {
      "name": { "pattern": "^@comity/" }
    }
  },
  "then": {
    "required": ["comity"],
    "errorMessage": {
      "required": "Comity-scoped modules (@comity/) must declare a Quality Domain in comity.qualityDomain"
    }
  },
  "else": {
    "properties": {
      "comity": {
        "not": {
          "required": ["qualityDomain"]
        }
      }
    },
    "errorMessage": {
      "properties": {
        "comity": "Non-Comity modules should not declare Quality Domains"
      }
    }
  }
}
