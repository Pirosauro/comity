import { Context } from "@comity/core/patterns";

/**
 * Application context for managing module lifecycle hooks and events.
 *
 * @remarks
 * Extends the base Context from @comity/core/patterns to provide
 * application-specific functionality for module initialization and communication.
 */
export class ApplicationContext extends Context {
  // For future extensions
}
