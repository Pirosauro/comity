/**
 * Current version of the Comity CLI package.
 *
 * @remarks
 * This version string is used by the CLI to display version information
 * and should match the version in package.json. In production builds,
 * this should ideally be dynamically set from the package.json version
 * or an environment variable.
 *
 * @example
 * ```typescript
 * import { VERSION } from "@comity/cli";
 * console.log(`Comity CLI v${VERSION}`);
 * ```
 */
export const VERSION = "1.0.0"; // Ideally, this should be dynamically set from package.json or an environment variable
